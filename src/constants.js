export const SCENE = Object.freeze(
{
    TITLE: 'title',
    GAME: 'game'
});

export const GAMEPLAY = Object.freeze({
    INITIAL_SCENE: SCENE.TITLE,
    WIDTH: 256,
    HEIGHT: 256,
    GRAVITY: 0.098
});